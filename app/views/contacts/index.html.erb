 <div class="container">
<!--<button type="button" class="btn btn-primary btn-lg">Large button</button>-->
<!--<button type="button" class="btn btn-secondary btn-lg">Large button</button>-->
  <div class="contents">
            <ul class="row">
             
<h1>List of Duffs</h1>
<div class="table-responsive">
  <table class="table">
<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Content</th>
    
  </tr>

<% @contacts.each do |contact| %>
  <tr>
    <td><%= contact.name %></td>
    <td><%= contact.email %></td>
     <td><%= contact.content %></td>
   <td><%= link_to 'Check the details', contact_path(contact.id) %></td>
   
  
    <!--<td><%= link_to link_to "Edit this duff", edit_contact_path(contact.id) %></td>-->
    <!--<td><%= link_to link_to 'Delete this duff', contact_path(contact.id), method: :delete ,data: { confirm: 'Are you sure?' } %></td>-->
    <td><%= link_to 'Post a new duff', new_contact_path %></td>
  </tr>
<% end %>
 
</table>
</table>
</div>
<br>



        </div>
      
    
</div>